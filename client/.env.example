# Optional — only needed if the API is not on the same origin as the dev server.
# Leave unset when using Vite proxy (default): requests go to /api → localhost:3000

# VITE_API_URL=http://localhost:3000/api

# Production (Railway frontend service example):
# VITE_API_URL=https://your-api-service.up.railway.app/api
