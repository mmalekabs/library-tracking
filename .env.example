# This project keeps secrets in server/.env only.
# Copy the example file and fill in your own values locally:
#
#   cd server
#   copy .env.example .env    (Windows)
#   cp .env.example .env      (macOS/Linux)
#
# See server/.env.example for every variable and README.md for setup.
