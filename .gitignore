# Dependencies
node_modules/

# Build output
dist/
build/
client/dist/
server/dist/

# Environment & secrets (never commit real values)
.env
.env.*
!.env.example
!**/.env.example
server/.env
client/.env

# Credentials & keys
*.pem
*.key
credentials.json
secrets.json
service-account*.json

# Logs & debug
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS & editors
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Test / coverage
coverage/
.nyc_output/

# Prisma (generated client lives in node_modules)
# Migrations ARE committed — only local .env is excluded

# Uploads / local data (if added later)
uploads/
*.sqlite
*.db
